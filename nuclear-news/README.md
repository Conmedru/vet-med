# нуклеар.ру

Автоматизированная платформа для сбора, обработки и публикации новостей ядерной медицины.

## Архитектура

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Источники  │ ──▶ │   Scraper   │ ──▶ │  AI Layer   │ ──▶ │   Admin     │
│  (RSS/Web)  │     │   Layer     │     │  (GPT-4o)   │     │   Panel     │
└─────────────┘     └─────────────┘     └─────────────┘     └──────┬──────┘
                                                                   │
                                                                   ▼
                                                            ┌─────────────┐
                                                            │  Astro SSG  │
                                                            │  (Public)   │
                                                            └─────────────┘
```

## Стек технологий

| Компонент | Технология |
|-----------|------------|
| Frontend (Public) | Astro + React |
| Frontend (Admin) | React + TanStack Query |
| Backend | FastAPI (Python) |
| Database | PostgreSQL + pgvector |
| Queue | Redis + Celery |
| AI | OpenAI GPT-4o / Anthropic Claude |

## Структура проекта

```
nuclear-news/
├── apps/
│   ├── web/          # Astro SSG - публичный сайт
│   ├── admin/        # React SPA - панель модерации
│   └── api/          # FastAPI - бэкенд
├── packages/         # Shared код
├── infra/            # Docker, Terraform
└── scripts/          # Утилиты
```

## Быстрый старт

### 1. Запуск инфраструктуры (Docker)

```bash
cd infra/docker
cp .env.example .env
# Добавить OPENAI_API_KEY в .env
docker-compose up -d
```

### 2. Миграция БД

```bash
cd apps/api
alembic upgrade head
```

### 3. Запуск API (dev)

```bash
cd apps/api
pip install -r requirements.txt
uvicorn app.main:app --reload
```

API доступен на http://localhost:8000

### 4. Документация API

- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## Источники новостей

| Источник | Тип адаптера | Статус |
|----------|--------------|--------|
| MedicalXpress | RSS | 🟡 В разработке |
| JNM (SNMMI) | RSS | 🔴 Не реализован |
| EJNMMI | Playwright | 🔴 Не реализован |
| EANM | PDF | 🔴 Не реализован |

## API Endpoints

### Articles

- `GET /articles/` — список статей
- `GET /articles/drafts` — черновики для модерации
- `GET /articles/{id}` — получить статью
- `PUT /articles/{id}` — редактировать статью
- `POST /articles/{id}/publish` — опубликовать

### Sources

- `GET /sources/` — список источников
- `POST /sources/` — добавить источник
- `POST /sources/{id}/scrape` — запустить парсинг

## Разработка

### Требования

- Python 3.12+
- Node.js 20+
- Docker & Docker Compose
- PostgreSQL 16 с pgvector

### Установка зависимостей

```bash
# Root (Turborepo)
npm install

# API
cd apps/api
pip install -r requirements.txt
playwright install chromium
```

## Лицензия

Proprietary
